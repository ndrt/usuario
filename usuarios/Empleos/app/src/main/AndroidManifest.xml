<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ndt.empleos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Empleos" />
        <activity android:name=".registrar" />
        <activity android:name=".registrarProfesionista" />
        <activity android:name=".registraTecnico" />
        <activity android:name=".verProfesionista" />
        <activity android:name=".verEmpleos" />
        <activity android:name=".verTecnico" />
        <activity android:name=".registraEstudiante"></activity>
    </application>

</manifest>
